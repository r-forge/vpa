#Generated by codetoolsBioC version 0.0.16
#Timestamp: Wed May 25 13:51:51 2011

#Imports: graphics, grDevices, stats, utils

importFrom(graphics, barplot)

importFrom(grDevices, as.raster, is.raster, replayPlot)

importFrom(stats, as.ts, coef, dendrapply, deviance, end, formula,
                      frequency, IQR, is.leaf, knots, na.omit, naprint, pf, pnorm,
                      print.anova, printCoefmat, pt, quantile, reformulate,
                      residuals, start, symnum, terms, time, ts, tsp,
                      write.ftable)

importFrom(utils, as.person, browseURL, head, menu, read.table, str,
                      tail, toBibtex, write.table)

export(pos2seq, read.vcf, write.vcf, vcfreq, subvcf, filtervcf, LoadFiltering, Patterning, getref, Pos2Gene, gefreq, filterpos)

S3method(read.vcf, default)

S3method(write.vcf, default)

S3method(print, vcf)

S3method(print, varlist)

S3method(summary, vcf)

S3method(LoadFiltering, default)
